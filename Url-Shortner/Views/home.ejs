<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Url - Shortner</title>

    <style>
        *{
            margin: 0;
        }
        .page{ 
            height: 100vh;
            width: 100vw;
            background-color: #23DC6C;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .box{
            background-color: #23DCC8;
            height: 80%;
            width: 85%;
            border-radius: 10px;
            border: 5px solid green;
            padding: 10px;
            display: flex;
            
            align-items: center;
            flex-direction: column;
            /* box-shadow: 20px 20px 5px green; */
        }

        .box p {
            padding: 10px;
            font-size: large;
            font-family: monospace;
        }
        .title{
            color: #21371f;
            text-align: center;
            margin-top: 10px;
            margin-bottom: 40px;
            font-family: 'Gill Sans', 'Gill Sans MT', Calibri, 'Trebuchet MS', sans-serif;
        }

        .inputdata {
            margin-top: 50px;
            display: flex;
            justify-content: center;
           
            
        }

        .inputdata input{
            padding: 10px;
            font-size: larger;
            width : 500px;
        }

        .inputdata button{
            font-size: larger;
        }
        .inline{
            
            display: flex;
        }

        .tabelData{
            margin: 50px;
            font-size: 20px;
        }

        
    </style>
</head>
<body>
    <div class="page">

        <div class="box">
            <h1 class="title">Welcome to URL Shortner</h1>

            <div class="inputdata">
                <form class="inline" method="post" action="/url">
                    <input type="text" name="url" placeholder="Enter your URL http://example.com/">
                    <button type="submit">Genearate</button>
                </form>
            </div>
            
            <% if (locals.shortid) { %>
                <center>
                <p>Short URL: http://localhost:8000/url/<%= locals.shortid %> for URL : <%= locals.url %></p>
                </center>
            <% }%>
            

            <% if (locals.urlss) { %>
            <div class="tabelData">
               <table border="2">
                <thead>
                    <th>
                        Sr. No
                    </th>

                    <th>
                        URL
                    </th>
                    
                    <th>
                            ID
                    </th>
                       
                    <th>
                        Clicks
                    </th>
                </thead>

                <tbody>
                    <% urlss.forEach((url, index) => { %>
                     <tr>
                        <td><%= index + 1 %></td>
                        <td><%= url.redirectURl %></td>
                        <td><%= url.shortID %></td>
                        <td><%= url.visitHistory.length %></td>
                        
                     </tr>
                    <% }) %>
                </tbody>
               </table>

            </div>   
            <% } %>

            
           

        </div>

        
    </div>
    
</body>
</html> 